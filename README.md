简单社交平台（Flask + MySQL）

快速开始：
1. 编辑 config.py（或复制 config.py.example）填写 MySQL 连接。
2. 安装依赖：pip install -r requirements.txt
3. 运行：python app.py

应用会在首次启动时自动建表并创建一个管理员(admin/password)和一个用户(user/password)以及示例动态。